# RAG-Eye 全功能演示指南（最终版本）

## 1. 演示目标

本演示用于完整展示 RAG-Eye 的最终能力：
- 全链路评测（消融、显著性、增益拆解、错误分析）
- 校准与拒答策略
- 决策门控（Decision Gate）
- 评分映射（Competition Scorecard）
- 工程质量护栏（unit/smoke/ruff/mypy）

## 2. 演示前准备

### 2.1 环境要求

- Python 3.11（建议）
- 网络可访问模型服务（若需要在线推理）

### 2.2 安装依赖

```bash
pip install -U -r requirements.txt
```

### 2.3 数据与配置确认

确认以下文件存在：
- `code/rag/outputs/large/rag_eval_results.json`
- `code/rag/outputs/small/rag_eval_results.json`
- `code/rag/outputs/generated_testset.json`
- `configs/task_v2.yaml`
- `configs/task_v2_groundtruth.yaml`

## 3. 演示主流程（推荐顺序）

### 步骤1：运行代理标签口径评测

```bash
python code/rag/run_pipeline.py --task-v2 --task-v2-config configs/task_v2.yaml --output-dir code/rag/reports/task_v2/latest
```

重点讲解：
- 这是默认工程口径（速度快、迭代友好）
- 输出了完整工件链

### 步骤2：运行 ground-truth 口径评测

```bash
python code/rag/run_pipeline.py --task-v2 --task-v2-config configs/task_v2_groundtruth.yaml --output-dir code/rag/reports/task_v2/groundtruth
```

重点讲解：
- 这是更严格的校准口径
- 可与默认口径对比，展示评测严谨性

### 步骤3：执行质量验证

```bash
python tests/unit/test_task_v2_quality.py
python tests/smoke/test_task_v2_pipeline.py
ruff check code/rag/task_v2 code/rag/task_v2_pipeline.py tests/unit/test_task_v2_quality.py
mypy --ignore-missing-imports code/rag/task_v2 code/rag/task_v2_pipeline.py
```

重点讲解：
- 不仅有算法结果，还有工程质量保障
- CI中同样执行这些关键校验

### 步骤4：打开 Dashboard v3

```bash
streamlit run code/rag/dashboard_v3.py
```

建议展示顺序：
1. Home（项目总览）
2. Rigor（显著性与置信区间）
3. Gain Decomposition（题型收益）
4. Error X-Ray（高风险样例）
5. Calibration（ECE/Brier/阈值扫描）
6. Decision Gate（PASS/FAIL与失败规则）
7. Competition Scorecard（评分映射与就绪度）
8. Repro & CI（复现与工程可信度）

## 4. 演示时建议重点话术

### 4.1 评测闭环

“我们不是只看一个分数，而是从数据、指标、统计显著性、风险控制、规则门控一直到评分映射形成闭环。”

### 4.2 可执行性

“Decision Gate直接给出准入结论，Competition Scorecard直接对接评审语言，方便做上线与比赛双决策。”

### 4.3 严谨性

“同一套流程可跑代理标签和ground-truth两种校准口径，保证效率和严谨兼顾。”

## 5. 演示后可提交材料

建议打包以下目录/文件用于评审：
- `code/rag/reports/task_v2/latest/`
- `code/rag/reports/task_v2/groundtruth/`
- `README.md`
- `项目介绍文档_新版本.md`
- `项目文档_新版本.md`

## 6. 常见问题排查

1. 看板打不开
- 检查是否已安装 `streamlit`、`plotly`

2. 主流程报找不到文件
- 检查 `configs/*.yaml` 中输入路径是否存在

3. 校准结果异常偏低
- 先确认 `correctness_mode`（proxy vs ground_truth）
- 检查 `answer` 与 `ground_truth` 文本质量

4. ruff/mypy失败
- 按错误提示修复后重跑；CI同口径检查
