# RAG-Eye 技术说明文档（代码对齐版）

本文档基于当前仓库 `E:\传智杯\czbAiTest` 的真实代码与配置整理，覆盖系统架构、核心流程、关键模块、评测体系、API、配置、产物与演示方式。

## 1. 项目概述

RAG-Eye 是一个面向垂直领域标准文档的 RAG 评测与优化系统，当前知识源为 `code/rag/data/GBT+44510-2024.pdf`。

项目核心目标：

1. 让“检索增强问答”不只是能跑，而是可验证、可复现、可解释。
2. 通过 Hybrid Retrieval、Query Rewrite、HyDE、Cross-Encoder Rerank、Reflection 等模块提升回答质量。
3. 通过真实消融、显著性检验、分类型收益分析、错误分型、校准与门禁判定形成实验闭环。
4. 通过 Streamlit Dashboard 与 FastAPI 接口支撑演示、分析与复盘。

---

## 2. 代码结构总览

### 2.1 顶层目录

| 路径 | 作用 |
|---|---|
| `code/rag/` | 核心代码、API、评测、报告、仪表盘 |
| `configs/` | Task-v2 配置 |
| `benchmark/` | Benchmark 协议、Schema、Split 元数据 |
| `tests/smoke/` | Task-v2 冒烟测试 |
| `tests/unit/` | Task-v2 质量单测 |
| `README.md` | 项目总览 |

### 2.2 核心文件

| 文件 | 作用 |
|---|---|
| `code/rag/api.py` | 在线检索、问答、反思、Phase3/4 接口 |
| `code/rag/rag.py` | 基础 RAG 流程 |
| `code/rag/eval_engine.py` | 基础评测器 |
| `code/rag/export_report.py` | 基础报告导出 |
| `code/rag/run_pipeline.py` | 一键入口（模型拉取 / 基础报告 / Task-v2） |
| `code/rag/task_v2_pipeline.py` | Task-v2 总流水线 |
| `code/rag/task_v2/*.py` | Task-v2 子模块（ablation / 统计 / 收益 / 错误 / 校准 / 门禁 / 评分映射） |
| `code/rag/dashboard.py` | 早期看板 |
| `code/rag/dashboard_pro.py` | 中期增强版看板 |
| `code/rag/dashboard_v3.py` | 最终展示版看板 |

---

## 3. 系统架构

系统可分为 5 层：

1. 数据层
2. 检索层
3. 生成层
4. 评测与分析层
5. 报告与展示层

### 3.1 数据层

- 原始文档：`code/rag/data/GBT+44510-2024.pdf`
- 测试集：`code/rag/outputs/generated_testset.json`
- 基线评测结果：`code/rag/outputs/small/`、`code/rag/outputs/large/`
- VectorStore：`code/rag/vectorstores/ev_small_chunks/`、`code/rag/vectorstores/ev_large_chunks/`

### 3.2 检索层

- Dense Retrieval：`Chroma + HuggingFaceEmbeddings`
- Sparse Retrieval：`BM25Retriever`
- Fusion：`EnsembleRetriever`
- Query Enhancement：Multi-Query、HyDE
- Rerank：`sentence_transformers.CrossEncoder`

### 3.3 生成层

- 使用 OpenAI-compatible LLM
- 基于证据生成回答
- Reflection/Judge 对回答置信度评估并决定是否重试

### 3.4 评测与分析层

- 基础指标：Context Recall、Context Precision、Faithfulness、Answer Relevance
- Phase3：多角色 Judge、Hard Negative、层级分类
- Phase4：消融模拟、错误分析、Long Context Probe
- Task-v2：真实消融、显著性检验、Query Type 收益、错误看板、答案校准、决策门禁、比赛评分映射

### 3.5 报告与展示层

- Markdown 报告
- CSV/JSON 工件
- Streamlit Dashboard
- FastAPI 接口

---

## 4. 核心运行流程

### 4.1 在线问答流程（`/retrieve` / `/ask`）

1. 输入问题
2. 可选 Query Rewrite / Multi-Query
3. 可选 HyDE 生成假设答案增强检索
4. Dense + BM25 融合召回
5. 去重
6. Cross-Encoder 重排
7. 基于证据生成回答
8. Reflection 评估并按阈值重试（`/ask_with_reflection`）

### 4.2 基础评测流程（`rag.py` + `eval_engine.py`）

1. 构建向量库
2. 读取测试集
3. 对每个问题执行 RAG 推理
4. 收集回答与检索上下文
5. 评测四项核心指标
6. 保存 JSON / CSV

### 4.3 Task-v2 流程（`task_v2_pipeline.py`）

1. 读取 `configs/task_v2.yaml`
2. 加载 variants 对应评测结果
3. 生成真实 ablation 表
4. 进行显著性检验（Wilcoxon + Bootstrap CI + Cohen's d）
5. 计算 Query Type 收益
6. 构建错误看板与高风险样例
7. 计算校准指标与阈值扫描
8. 生成 Decision Gate 与 Competition Scorecard
9. 导出 `summary.json`、`csv`、`report.md`

---

## 5. 关键模块说明

### 5.1 `env_utils.py`

- 自动向上查找 `.env`
- 强制要求 `OPENAI_API_KEY`
- 提供 `OPENAI_BASE_URL`、`OPENAI_MODEL`、`HF_ENDPOINT`、`HF_HOME`、`RERANKER_MODEL`

### 5.2 `pull_models.py`

- 预拉取 `BAAI/bge-m3`
- 预拉取 `RERANKER_MODEL`（默认 `BAAI/bge-reranker-base`）

### 5.3 `api.py`

提供在线服务：

- `/retrieve`
- `/ask`
- `/ask_with_reflection`
- `/phase3/evaluate_sample`
- `/phase4/ablation`
- `/phase4/error_analysis`
- `/phase4/long_context_probe`

### 5.4 `phase3_eval.py`

- 样本层级分类
- Hard Negative 生成
- 四角色 Judge：retriever / generator / safety / meta

### 5.5 `phase4_tools.py`

- 基础消融模拟
- 错误分桶
- 长上下文探针生成

### 5.6 `export_report.py`

- 从评测 JSON 提取基础指标
- 生成 `summary.json`、`ablation.csv`、`error_summary.csv`、`report.md`

### 5.7 `task_v2/real_ablation.py`

- 读取每个 variant 的真实评测文件
- 聚合输出 `ablation_real.csv`

### 5.8 `task_v2/statistics.py`

- 配对 Wilcoxon 检验
- Bootstrap 95% CI
- Cohen's d

### 5.9 `task_v2/query_gain.py`

- 按 question 的 `q_type` 分桶统计收益
- 输出 `gain_by_query_type.csv`

### 5.10 `task_v2/error_analysis.py`

- 错误标签：
  - `no_recall`
  - `bad_rank`
  - `good_retrieval_bad_generation`
  - `over_retrieval_noise`
  - `ok`
- 输出 `error_dashboard.csv` 与 `error_cases_topk.json`

### 5.11 `task_v2/calibration.py`

- 计算 ECE、Brier、拒答率、错误捕获率、错误泄漏率等指标
- 支持阈值扫描，输出 `calibration_threshold_sweep.csv`

### 5.12 `task_v2/decision_gate.py`

- 基于指标下限、显著性、校准约束生成门禁结果
- 输出：
  - `decision_gate_summary.csv`
  - `decision_gate_metric_detail.csv`

### 5.13 `task_v2/competition_scorecard.py`

- 将门禁规则映射为比赛评分项
- 输出：
  - `competition_scorecard_summary.csv`
  - `competition_scorecard_detail.csv`

---

## 6. 指标与统计方法

### 6.1 核心指标

| 指标 | 含义 |
|---|---|
| `context_recall` | 检索是否覆盖关键证据 |
| `context_precision` | 召回内容是否足够干净 |
| `faithfulness` | 回答是否忠于证据 |
| `answer_relevance` | 回答是否紧扣问题 |

### 6.2 Task-v2 统计方法

- 以 baseline variant 为参照
- 对齐相同 question 集
- 配对 Wilcoxon 检验
- Bootstrap 95% CI
- Cohen's d 效应量

### 6.3 Decision Gate 规则来源（`configs/task_v2.yaml`）

- `metric_minimums`：指标下限
- `require_significant_improvement_metrics`：要求显著提升的指标
- `max_ece`
- `min_accepted_accuracy_proxy`
- `max_error_leakage_rate_after_accept`

---

## 7. 配置说明

### 7.1 `.env`

必需项：

```env
OPENAI_API_KEY=your_key
```

常用项：

```env
OPENAI_BASE_URL=https://api.deepseek.com/v1
OPENAI_MODEL=deepseek-chat
HF_ENDPOINT=https://hf-mirror.com
HF_HOME=.cache/huggingface
RERANKER_MODEL=BAAI/bge-reranker-base
```

### 7.2 `configs/task_v2.yaml`

包含：

- `experiment`
- `benchmark`
- `variants`
- `calibration`
- `decision_gate`
- `competition_scorecard`

### 7.3 Benchmark 约定

- `benchmark/README.md`
- `benchmark/benchmark_schema.json`
- `benchmark/splits/manifest.json`

当前 split 策略（manifest 描述）：

- 前 20 条：dev
- 中 20 条：test
- 后 20 条：challenge

---

## 8. 输出产物

### 8.1 基础报告产物（`export_report.py`）

- `summary.json`
- `ablation.csv`
- `error_summary.csv`
- `report.md`

### 8.2 Task-v2 产物（`task_v2_pipeline.py`）

- `summary.json`
- `ablation_real.csv`
- `stats_significance.csv`
- `gain_by_query_type.csv`
- `error_dashboard.csv`
- `error_cases_topk.json`
- `calibration_metrics.csv`
- `calibration_detail.csv`
- `calibration_threshold_sweep.csv`
- `decision_gate_summary.csv`
- `decision_gate_metric_detail.csv`
- `competition_scorecard_summary.csv`
- `competition_scorecard_detail.csv`
- `report.md`

---

## 9. API 接口说明

### 9.1 `/retrieve`

- 输入：`question`、`use_hyde`、`use_multi_query`
- 输出：`expanded_queries`、`documents`

### 9.2 `/ask`

- 输入同上
- 输出：`answer`、`expanded_queries`、`documents`

### 9.3 `/ask_with_reflection`

- 额外输入：`confidence_threshold`、`max_retries`
- 额外输出：`confidence`、`needs_retry`、`retries_used`、`judge_reason`

### 9.4 `/phase3/evaluate_sample`

- 输入：`question`、`ground_truth`、`answer`、`retrieved_contexts`、`q_type`
- 输出：`layer`、`hard_negatives`、四角色 judge 结果

### 9.5 `/phase4/ablation`

- 输入：`eval_json_path`
- 输出：消融模拟 variant 列表

### 9.6 `/phase4/error_analysis`

- 输入：`eval_json_path`
- 输出：错误统计 summary

### 9.7 `/phase4/long_context_probe`

- 输入：`context_length`、`position`
- 输出：长上下文探针样例

---

## 10. 仪表盘与展示层

当前项目包含三版 Streamlit 页面：

1. `dashboard.py`（早期）
2. `dashboard_pro.py`（增强）
3. `dashboard_v3.py`（最终展示）

`dashboard_v3.py` 当前页面：

- Home
- Overview
- Rigor
- Gain Decomposition
- Error X-Ray
- Calibration
- Decision Gate
- Competition Scorecard
- Method Graph
- Repro & CI

---

## 11. 质量控制

### 11.1 快速失败原则

- 缺失环境变量直接报错
- 缺失输入文件直接报错
- 配置字段错误直接报错
- 评测记录关键字段缺失直接报错

### 11.2 冒烟测试

`tests/smoke/test_task_v2_pipeline.py` 检查：

- Task-v2 主流水线可执行
- 关键产物存在（含 stats/calibration/gate/scorecard）
- full variant 指标与输入评测文件一致

### 11.3 单元质量测试

`tests/unit/test_task_v2_quality.py` 覆盖：

- calibration 不同 correctness mode
- threshold sweep
- decision gate 基本规则判定

### 11.4 CI

`.github/workflows/ci.yml` 当前执行：

- `lint-type`：ruff + mypy（task_v2 范围）
- `smoke-eval`：smoke test + unit quality tests

---

## 12. 演示与运行命令

### 12.1 模型预拉取

```bash
python code/rag/pull_models.py
```

### 12.2 启动 API

```bash
uvicorn code.rag.api:app --host 0.0.0.0 --port 8000
```

### 12.3 启动 Dashboard

```bash
streamlit run code/rag/dashboard_v3.py
```

### 12.4 一键导出基础报告

```bash
python code/rag/run_pipeline.py --eval-json code/rag/outputs/large/rag_eval_results.json --output-dir code/rag/reports/latest
```

### 12.5 运行 Task-v2

```bash
python code/rag/run_pipeline.py --task-v2 --task-v2-config configs/task_v2.yaml --output-dir code/rag/reports/task_v2/latest
```

### 12.6 运行冒烟测试

```bash
python tests/smoke/test_task_v2_pipeline.py
```

### 12.7 运行质量单测

```bash
python tests/unit/test_task_v2_quality.py
```

---

## 13. 结论

当前项目已经从基础 RAG Demo 升级为具备评测闭环的工程化平台：

- 可运行的在线问答能力
- 可复现的配置与流水线
- 可统计检验的优化结果
- 可解释的错误归因
- 可量化的校准与拒答策略
- 可执行的门禁判定与评分映射
- 可持续演进的 CI 与 benchmark 基础

本版文档已与当前仓库代码、配置、命令和产物保持一致，可直接用于展示与复现。
